import actions, {TYPES} from 'component/CountButton/action.js';

describe('test1', function () {
    
    it('good', function () {
        
        console.log(TYPES);
        expect(1).to.equal(1);
    
    });
    
});